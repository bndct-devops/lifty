# Frontend (lifty)

Run frontend dev server:

```bash
cd frontend
npm install
npm run dev
```

By default the frontend expects the backend to be available on the same origin at the `/api` paths. For local development you can run the backend on `localhost:8000` and use a simple proxy in Vite if desired.
